import { useEffect, useState } from 'react'
import { addCurriculumCourse, createCourse, createCurriculum, getCourses, getCurricula, getCurriculumCourses, type Course, type Curriculum, type CurriculumCourse } from '../api/curriculumManagement'

export function CurriculumManagement({ canManage }: { canManage: boolean }) {
  const [curricula,setCurricula]=useState<Curriculum[]>([]),[courses,setCourses]=useState<Course[]>([]),[selected,setSelected]=useState(''),[mappings,setMappings]=useState<CurriculumCourse[]>([]),[error,setError]=useState(''),[saving,setSaving]=useState(false)
  const [cf,setCf]=useState({programmeId:'',version:'1.0',title:'',minimumCredits:'',effectiveFrom:'',effectiveTo:''})
  const [uf,setUf]=useState({code:'',name:'',creditUnits:'',description:'',courseType:''}),[mf,setMf]=useState({courseId:'',yearOfStudy:'1',semesterNumber:'1',isCore:true})
  useEffect(()=>{if(canManage) void load()},[canManage])
  async function load(){try{setError('');const [a,b]=await Promise.all([getCurricula(),getCourses()]);setCurricula(a);setCourses(b)}catch(e){setError(e instanceof Error?e.message:'Unable to load curriculum data.')}}
  async function refreshMappings(id:string){setSelected(id);if(id) try{setMappings(await getCurriculumCourses(id))}catch(e){setError(e instanceof Error?e.message:'Unable to load mappings.')}}
  async function saveCurriculum(e:React.FormEvent){e.preventDefault();setSaving(true);try{await createCurriculum({...cf,minimumCredits:Number(cf.minimumCredits)});setCf({...cf,version:'1.0',title:'',minimumCredits:'',effectiveFrom:'',effectiveTo:''});await load()}catch(e){setError(e instanceof Error?e.message:'Unable to save curriculum.')}finally{setSaving(false)}}
  async function saveCourse(e:React.FormEvent){e.preventDefault();setSaving(true);try{await createCourse({...uf,creditUnits:Number(uf.creditUnits)});setUf({...uf,code:'',name:'',creditUnits:'',description:'',courseType:''});await load()}catch(e){setError(e instanceof Error?e.message:'Unable to save course.')}finally{setSaving(false)}}
  async function saveMapping(e:React.FormEvent){e.preventDefault();if(!selected)return;setSaving(true);try{await addCurriculumCourse(selected,{...mf,yearOfStudy:Number(mf.yearOfStudy),semesterNumber:Number(mf.semesterNumber)});await refreshMappings(selected)}catch(e){setError(e instanceof Error?e.message:'Unable to map course.')}finally{setSaving(false)}}
  if(!canManage)return null
  return <section className="panel"><div className="panel-heading"><div><p className="eyebrow">Academic Structure</p><h2>Curriculum & Courses</h2></div><span>{curricula.length} curricula · {courses.length} courses</span></div>{error&&<div className="error">{error}</div>}<div className="summary-grid"><form className="student-form" onSubmit={saveCurriculum}><h3>New curriculum version</h3><input required placeholder="Programme UUID" value={cf.programmeId} onChange={e=>setCf({...cf,programmeId:e.target.value})}/><div className="form-row"><input required placeholder="Version" value={cf.version} onChange={e=>setCf({...cf,version:e.target.value})}/><input required placeholder="Title" value={cf.title} onChange={e=>setCf({...cf,title:e.target.value})}/></div><div className="form-row"><input type="number" min="0" placeholder="Minimum credits" value={cf.minimumCredits} onChange={e=>setCf({...cf,minimumCredits:e.target.value})}/><input required type="date" value={cf.effectiveFrom} onChange={e=>setCf({...cf,effectiveFrom:e.target.value})}/></div><button disabled={saving}>Add curriculum</button></form><form className="student-form" onSubmit={saveCourse}><h3>Course/unit catalogue</h3><div className="form-row"><input required placeholder="Unit code" value={uf.code} onChange={e=>setUf({...uf,code:e.target.value})}/><input required placeholder="Unit name" value={uf.name} onChange={e=>setUf({...uf,name:e.target.value})}/></div><div className="form-row"><input required type="number" min="0" placeholder="Credit units" value={uf.creditUnits} onChange={e=>setUf({...uf,creditUnits:e.target.value})}/><input placeholder="Course type" value={uf.courseType} onChange={e=>setUf({...uf,courseType:e.target.value})}/></div><button disabled={saving}>Add course</button></form></div><div className="form-row"><select value={selected} onChange={e=>void refreshMappings(e.target.value)}><option value="">Select curriculum</option>{curricula.map(c=><option key={c.id} value={c.id}>{c.title} · v{c.version}</option>)}</select>{selected&&<form className="form-row" onSubmit={saveMapping}><select required value={mf.courseId} onChange={e=>setMf({...mf,courseId:e.target.value})}><option value="">Select course</option>{courses.map(c=><option key={c.id} value={c.id}>{c.code} — {c.name}</option>)}</select><input type="number" min="1" value={mf.yearOfStudy} onChange={e=>setMf({...mf,yearOfStudy:e.target.value})} placeholder="Year"/><input type="number" min="1" value={mf.semesterNumber} onChange={e=>setMf({...mf,semesterNumber:e.target.value})} placeholder="Semester"/><label><input type="checkbox" checked={mf.isCore} onChange={e=>setMf({...mf,isCore:e.target.checked})}/> Core</label><button disabled={saving}>Map course</button></form>}</div><div className="table-wrap"><table><thead><tr><th>Course</th><th>Year</th><th>Semester</th><th>Type</th></tr></thead><tbody>{mappings.map(m=>{const c=courses.find(x=>x.id===m.courseId);return <tr key={m.id}><td>{c?.code} — {c?.name}</td><td>{m.yearOfStudy}</td><td>{m.semesterNumber}</td><td>{m.isCore?'Core':'Elective'}</td></tr>})}{selected&&!mappings.length&&<tr><td colSpan={4} className="empty">No courses mapped to this curriculum.</td></tr>}</tbody></table></div></section>
}
